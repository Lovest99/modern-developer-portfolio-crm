export default {
  plugins: {
    autoprefixer: {
      // Add options here if needed
      // For example, to target specific browsers:
      // browsers: ['last 2 versions', 'Safari >= 9', 'iOS >= 9', 'not IE <= 11']
    },
  },
};
